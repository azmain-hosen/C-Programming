#include <stdio.h>
#include <stdbool.h>

int main(void)
{
    bool is_student = true;
    bool is_married = false;

    printf("Is student  :  %d\n", is_student);
    printf("Is married  :  %d\n", is_married);

    return 0;
}